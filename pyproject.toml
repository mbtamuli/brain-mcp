[project]
name = "brain-mcp"
version = "0.1.0"
description = "Brain MCP server and CLI"
requires-python = ">=3.10"
dependencies = [
    "mcp",
]

[tool.setuptools]
py-modules = ["brain", "build", "query", "serve", "sync", "write"]

[project.scripts]
brain = "brain:main"
